# TODO in ansible

* Add gem installs:
  * bundler
* Add npm installs:
  * yarn
  * webpack
  * create-react-app
  * all linter packages

* Add dotfile configuration (symlinks)
* Setup neovim correctly
* Setup zsh correctly
* Install Postgres.App
* Add psequel

